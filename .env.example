# Copy to .env and fill in. Used by docker-compose and local backend runs.

# dev | prod. In prod, PLEXTRAKTBOX_SECRET_KEY is required.
PLEXTRAKTBOX_ENV=dev

# Signs session cookies and derives the token-encryption (Fernet) key.
# Generate: python -c "import secrets; print(secrets.token_urlsafe(48))"
PLEXTRAKTBOX_SECRET_KEY=

# Where the SQLite DB and caches live.
PLEXTRAKTBOX_DATA_DIR=./data

# Optional
# PLEXTRAKTBOX_LOG_LEVEL=INFO
# PLEXTRAKTBOX_HOST=0.0.0.0
# PLEXTRAKTBOX_PORT=8000
